#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Only run tests if there are changes to test files or source files
CHANGED_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E "\.(js|liquid|json)$" || true)

if [ -n "$CHANGED_FILES" ]; then
  echo "📝 Changes detected in: $CHANGED_FILES"
  echo "Running quick checks..."
  
  # You can add linting or other quick checks here
  # For now, just a reminder
  echo "💡 Remember to run tests before pushing: npm test"
else
  echo "✅ No JS/Liquid files changed"
fi
